{
  "name": "boid-wars",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n server,wasm,client npm:dev:*",
    "dev:server": "cd server && cargo watch -x run",
    "dev:wasm": "cd lightyear-wasm && cargo watch -- wasm-pack build --target web --out-dir ../client/src/wasm",
    "dev:client": "cd client && npm run dev",
    "build": "npm run build:wasm && npm run build:client",
    "build:wasm": "cd lightyear-wasm && wasm-pack build --target web --out-dir ../client/src/wasm",
    "build:server": "cd server && cargo build --release",
    "build:client": "cd client && npm run build",
    "clean": "cargo clean && rm -rf client/node_modules client/dist lightyear-wasm/pkg"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}